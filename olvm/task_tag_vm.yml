---

- name: "Insert all tags for VM"
  ovirt.ovirt.ovirt_tag:
    auth: "{{ ovirt_auth }}"
    name: "{{ item }}"
    state: attached
    vms:
      - "{{ vm_node }}"
  with_items: "{{ hostvars[vm_node]['vm_tags'] }}"
